let protoTransport;

const addProtoTransport = (protoTransport) => {
  protoTransport = protoTransport;
};

const getProtoTransport = () => {
  return protoTransport;
};

///////
module.exports = {
  addProtoTransport,
  getProtoTransport,
};
